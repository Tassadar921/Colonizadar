enum RoomStatusEnum {
    OPENED = 'opened',
    CLOSED = 'closed',
    STARTING = 'starting',
    PLAYING = 'playing',
    WAITING = 'waiting',
    FINISHED = 'finished',
}

export default RoomStatusEnum;
