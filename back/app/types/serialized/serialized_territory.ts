type SerializedTerritory = {
    code: string;
    name: string;
    isCoastal: boolean;
    isFactory: boolean;
    createdAt?: string;
    updatedAt?: string;
};

export default SerializedTerritory;
